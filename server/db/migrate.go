// Package db — migrations are now handled by Goose via `make migration-up`.
// This file is retained as a stub so the package still compiles.
// Run: make migration-up   (uses Goose against DATABASE_URL from .env.local)
package db

